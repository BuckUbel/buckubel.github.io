import {LocalStoreType} from "./LocalStoreContext";

export const defaultLocalStoreState: LocalStoreType = {
  databaseActionCount: {},
  isSomeLoaded: false,
  database: {},
  viewDatabase: {},
  count: {},
  filterCount: {},
}
