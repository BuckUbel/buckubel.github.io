import React from 'react';
import styled from 'styled-components';

interface TableDateCellProps {
  classname?: string;
}

function DateTableCell(props: TableDateCellProps) {
  return (
    <>
    </>
  );
}

export default styled(DateTableCell)``;
