import React from 'react';
import styled from 'styled-components';

interface NumberTableCellProps {
  classname?: string;
}

function NumberTableCell(props: NumberTableCellProps) {
  return (
    <>
    </>
  );
}

export default styled(NumberTableCell)``;
