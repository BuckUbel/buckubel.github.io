import React from "react";
import styled from "styled-components";

interface StringInputFieldProps {
  className?: string;
}

function StringInputField(props: StringInputFieldProps) {
  return <></>;
}

export default styled(StringInputField)``;
