import React from "react";
import styled from "styled-components";

interface GameEntityDisplayProps {
  className?: string;
}

function GameEntityDisplay(props: GameEntityDisplayProps) {
  return <></>;
}

export default styled(GameEntityDisplay)``;
